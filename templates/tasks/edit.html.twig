{% extends 'base.html.twig' %}

{% block title %}Edit Task
{% endblock %}

{% block body %}
	<div class="task-edit-wrapper">
		<h1>Edit Task</h1>

		<form action="{{ path('tasks_update', { 'id': task.id }) }}" method="POST">
			<div class="form-group">
				<label for="task">📝 Task</label>
				<input type="text" id="task" name="name" class="form-control" value="{{ task.name }}" required/>
			</div>

			<div class="form-group">
				<label for="date">📅 Set Date</label>
				<input type="date" id="date" name="date" class="form-control" value="{{ task.date|date('Y-m-d') }}" required/>
			</div>

			<div class="mt-3">
				<button type="submit" class="btn btn-warning w-100">✏️ Modify Task</button>
			</div>
		</form>

		<div class="mt-3 text-center">
			<a href="{{ path('tasks_index') }}" class="btn btn-outline-secondary">⬅️ Back to Task List</a>
		</div>
	</div>

	<style>
		.task-edit-wrapper {
			max-width: 600px;
			margin: 50px auto;
			padding: 30px;
			background-color: #fdfdfd;
			border-radius: 12px;
			box-shadow: 0 0 12px rgba(0, 0, 0, 0.1);
			text-align: center;
		}

		.task-edit-wrapper h1 {
			margin-bottom: 25px;
			color: #333;
			font-weight: bold;
		}

		.form-group {
			text-align: left;
			margin-bottom: 20px;
		}

		.form-group label {
			font-weight: 500;
			display: block;
			margin-bottom: 5px;
		}

		.form-control {
			border-radius: 6px;
			border: 1px solid #ccc;
			padding: 10px;
		}
	</style>
{% endblock %}
